/*
Modifique este archivo y ejecute eve. Mira lo que pasó.

Es simple. Si algún archivo .in está vacío, se omitirá.

Intente llenar algunos archivos .in con datos y vea qué sucede.

Proceda con no-compare.c
*/

#include <stdio.h>

int main(void) {
  int a, b;
  scanf("%d%d", &a, &b);
  printf("%d\n", a*b);
  return 0;
}
